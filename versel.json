{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
{
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
{
  "routes": [
    {
      "src":"/",
      "dest":"/"
    },
        {
      "src":"/service",
      "dest":"/"
    }
  ]
}
{
  "routes": [
    {
      "src": "/[^.]+",
      "dest": "/",
      "status": 200
    }
  ]
}